- provide(:title, "Auction Index")

%h3.subheader.text-center Open Auctions

%table
  %thead
    %tr
      %th
        Title
      %th
        Starting Bid
      %th
        Buy Out
      %th
        End Date
  %tbody
    = render @auctions
= will_paginate @auctions
